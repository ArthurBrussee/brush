{
  "name": "brush",
  "version": "1.0.0",
  "description": "Brush",
  "private": true,
  "scripts": {
    "dev": "cd apps/brush-app/web && npm run dev",
    "build": "cd apps/brush-app/web && npm run build"
  },
  "workspaces": [
    "apps/brush-app/web"
  ],
  "devDependencies": {
    "baseline-browser-mapping": "^2.10.18"
  }
}
